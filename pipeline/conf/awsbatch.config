/*
 * AWS Batch executor configuration (optional)
 */

process {
    executor    = 'awsbatch'
    queue       = params.aws_queue ?: 'default'
}

aws {
    region      = params.aws_region ?: 'us-east-1'
    batch {
        cliPath = '/home/ec2-user/miniconda/bin/aws'
    }
}
