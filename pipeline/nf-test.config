config {
    // nf-test configuration for the phage/plasmid pipeline
    // Run: nf-test test from the project root (one level above pipeline/)

    testsDir    "../tests"       // test files live in project-root/tests/
    workDir     ".nf-test/work"

    // Default options applied to every test
    // Override per-test with options "..."
}
