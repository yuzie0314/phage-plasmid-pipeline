config {
    // nf-test runs from the project root (where this file lives)
    // test files: tests/modules/*.nf.test, tests/subworkflows/*.nf.test
    // ${projectDir} resolves to the project root in all test files
    testsDir "tests"
    workDir  ".nf-test/work"
}
